export const formatPagerOptionLabel = (index: number, title: string) =>
  `${index + 1}. ${title}`;

export const formatPagerSelectedLabel = (index: number, title: string) =>
  `${index + 1}. ${title}`;
